# Quiz Name
> ### Programmers / [level 2] <a href = "https://school.programmers.co.kr/learn/courses/30/lessons/12978?language=java"> 배달 </a>

<br>

## 💡 approaches
>  - 다익스트라 알고리즘

<br>

## 🔑 quiz solution

```py
from heapq import *

def solution(N, road, K):
    INF = int(1e9)
    start = 1
    answer = 0
    distance = [INF] * (N+1)
    graph = [[] for _ in range(N+1)]
    q = []
    
    for a, b, c in road:
        graph[a].append((b, c))
        graph[b].append((a, c))
    
    distance[start] = 0

    # 우선순위 큐 기준 : 거리
    heappush(q, (0, start))
    while q:
        dist, now = heappop(q)
        
        # 더 짧은 거리를 찾았다면
        if distance[now] < dist: continue
        if dist <= K: answer += 1
        
        for node, d in graph[now]:
            d += dist
            if distance[node] > d:
                distance[node] = d
                heappush(q, (d, node))

    return answer
```

```java
import java.util.*;

class Solution {
    public int solution(int N, int[][] road, int K) {
        int answer = 0;
        int[] distance = new int[N+1];
        // 노드의 개수는 변하지 않기 때문에 Array
        // 각 노드와 연결된 노드들의 개수는 동적이므로 List
        List<Node>[] graph = new ArrayList[N+1];
        PriorityQueue<Node> q = new PriorityQueue<>(new Comparator<Node>() {
            public int compare(Node o1, Node o2) {
                // 오름차순
                return o1.distance - o2.distance;
            }
        });
        
        for (int i=0;i < N+1;i++) {
            // null로 채워진 graph 배열에 ArrayList 객체 생성
            graph[i] = new ArrayList<>();
        }
        
        for (int[] temp: road) {
            int a = temp[0], b = temp[1], c = temp[2];
            graph[a].add(new Node(b, c));
            graph[b].add(new Node(a, c));
        }
        
        Arrays.fill(distance, Integer.MAX_VALUE);
        distance[1] = 0;

        q.add(new Node(1, 0));
        while (!q.isEmpty()) {
            Node now = q.remove();
            
            if (distance[now.num] < now.distance) continue;
            if (now.distance <= K) answer++;

            for (Node node: graph[now.num]) {
                int d = node.distance + now.distance;
                if (distance[node.num] > d) {
                    distance[node.num] = d;
                    q.add(new Node(node.num, d));
                }
            }
        }
        return answer;
    }
    
    class Node {
        int num; // 노드의 번호
        int distance; // 특정 노드와의 거리
        
        Node(int num, int distance) {
            this.num = num;
            this.distance = distance;
        }
    }
}
```
### Time Complexity : O(N)
## 👩🏻‍🏫 TIL
>  - 자바 int형 최대 값: Integer.MAX_VALUE
>  - 자바에서는 배열도 객체이기 때문에 제네릭 가능 -> List<int[]>
